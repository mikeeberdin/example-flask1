# Redis+Flask on Python 3.10

First step is to install the latest `docker` and `docker compose` for your platform and have that running.

To run this app... `./run.sh` and then visit <http://localhost:8080/>



